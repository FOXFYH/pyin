# 勋章系统 & 评级系统 - 移植指南

## 文件说明

| 文件 | 功能 | 依赖 |
|---|---|---|
| `badge-system.js` | 勋章/成就系统 | 无 |
| `grade-system.js` | 评级系统（SSS~D） | 无 |

两个模块完全独立，零依赖，可直接复制到任何项目使用。

---

## 勋章系统 BadgeSystem

### 引入

```html
<script src="badge-system.js"></script>
```

### 核心概念

勋章分两类：

1. **单场勋章**（BADGE_DEFS）：检查单场表现，概率触发
   - 如：完美无瑕（100%正确率）、五连绝世（连续5题）
   - `prob` 字段控制触发概率，0.7 = 70%概率获得

2. **积累型勋章**（CUMULATIVE_BADGE_DEFS）：检查累计数据，达到条件必定获得
   - 如：锲而不舍（累计10场）、学富五车（积分5000）
   - `prob` 固定为 1.0，不重复发放

### API

```js
// 检查本场所获奖章（会修改 studentData）
var earned = BadgeSystem.checkBadges(sessionStats, studentData);
// earned: [{ id, name, icon, desc, prob }, ...]

// 渲染勋章墙
BadgeSystem.renderBadges(badgesData, containerEl, options);
// badgesData: { badgeId: count, ... }
// options: { cardClass: 'badge-card', earnedClass: 'earned' }

// 获取定义
BadgeSystem.getAllDefs();        // 全部勋章定义
BadgeSystem.getSessionDefs();    // 单场勋章定义
BadgeSystem.getCumulativeDefs(); // 积累型勋章定义
```

### 数据格式

```js
// 单场数据（传入 checkBadges 的 sessionStats）
{
    accuracy: 85,          // 正确率 0~100
    maxStreak: 8,          // 最大连击
    avgTime: 6.5,          // 平均用时（秒）
    difficulty: 'medium',  // 难度
    earlyErrors: 1,        // 前几题错误数
    phaseCompleted: 'exam' // 阶段完成标记（用于积累型）
}

// 累计数据（传入 checkBadges 的 studentData，会被修改）
{
    sessions: 15,           // 总场次
    totalPoints: 3200,      // 总积分
    badges: { perfect: 2, streak5: 5 },  // 已获勋章
    easyCompleted: 2,       // 简单通关次数
    mediumCompleted: 1,     // 中等通关次数
    hardCompleted: 0        // 困难通关次数
}
```

### 自定义勋章

```js
// 添加单场勋章
BadgeSystem.BADGE_DEFS.push({
    id: 'my_badge',
    name: '我的勋章',
    icon: '🎯',
    desc: '自定义条件',
    check: function(s) { return s.myField > 10; },
    prob: 0.8
});

// 添加积累型勋章
BadgeSystem.CUMULATIVE_BADGE_DEFS.push({
    id: 'my_cumulative',
    name: '积累勋章',
    icon: '🌟',
    desc: '累计XX次',
    check: function(st) { return st.myCounter >= 100; },
    prob: 1.0
});
```

---

## 评级系统 GradeSystem

### 引入

```html
<script src="grade-system.js"></script>
```

### 核心概念

7个等级：SSS > SS > S > A > B > C > D

每个等级需同时满足**正确率**和**连击数**两个维度。等级越高，分数加成越大。

### API

```js
// 计算评级
var result = GradeSystem.calculate(accuracy, maxStreak, avgTime, config);
// result: { grade: 'SSS', bonus: 35.7 }

// 获取等级列表（用于UI展示）
var list = GradeSystem.getGradeList(config);
// list: [{ grade: 'SSS', minAcc: 97, minStreak: 15, bonusRange: '34.3~40%' }, ...]
```

### 配置项

```js
var config = {
    sssAcc: 97,      // SSS 最低正确率（默认97）
    sssStreak: 15,   // SSS 最低连击（默认15）
    maxBonus: 40     // 最高加成百分比（默认40）
};
```

配置只影响 SSS 的阈值，其他等级自动递减计算。

### 默认等级表

| 等级 | 最低正确率 | 最低连击 | 加成范围 |
|---|---|---|---|
| SSS | 97% | 15 | 34.3%~40% |
| SS | 90% | 10 | 28.6%~34.3% |
| S | 80% | 8 | 22.9%~28.6% |
| A | 70% | 5 | 17.1%~22.9% |
| B | 60% | 3 | 11.4%~17.1% |
| C | 40% | 0 | 5.7%~11.4% |
| D | 0% | 0 | 0%~5.7% |

---

## 完整使用示例

```html
<script src="badge-system.js"></script>
<script src="grade-system.js"></script>
<script>
// 一场比赛结束后
var sessionStats = {
    accuracy: 92,
    maxStreak: 12,
    avgTime: 4.8,
    difficulty: 'medium',
    earlyErrors: 1,
    phaseCompleted: ''
};

var studentData = {
    sessions: 0,
    totalPoints: 0,
    badges: {},
    easyCompleted: 0,
    mediumCompleted: 0,
    hardCompleted: 0
};

// 更新累计数据
studentData.sessions++;
studentData.totalPoints += 500;

// 计算评级
var grade = GradeSystem.calculate(sessionStats.accuracy, sessionStats.maxStreak, sessionStats.avgTime);
console.log('评级:', grade.grade, '加成:', grade.bonus + '%');

// 检查勋章
var earned = BadgeSystem.checkBadges(sessionStats, studentData);
earned.forEach(function(b) {
    console.log('获得勋章:', b.icon, b.name);
});

// 渲染勋章墙
BadgeSystem.renderBadges(studentData.badges, document.getElementById('badges-grid'));
</script>
```

---

## 移植检查清单

- [ ] 复制 `badge-system.js` 和/或 `grade-system.js` 到目标项目
- [ ] 在 HTML 中引入 script 标签
- [ ] 准备 sessionStats 和 studentData 数据结构
- [ ] 在合适的时机调用 checkBadges / calculate
- [ ] 添加勋章墙 CSS 样式（参考 style.css 中 `.badge-card` 相关）
- [ ] 添加评级展示 CSS 样式（参考 style.css 中 `.grade-*` 相关）
- [ ] 根据项目需求自定义勋章定义和评级配置
